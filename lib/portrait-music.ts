import playlist from "./portrait-playlist.json";

// Refresh with `node scripts/sync-portrait-playlist.mjs`.
export const portraitPlaylist = playlist;
export const portraitPlaylistUrl = playlist.url;
